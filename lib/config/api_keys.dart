// This file would contain API keys for external services (if needed)
// Currently, we're using a local solution that doesn't require API keys
// If we integrate with free APIs in the future, their keys would go here

class ApiKeys {
  // Reserved for future free API integrations if needed
  // Example:
  // static const String freeSpeechToTextApiKey = 'YOUR_API_KEY';
}
